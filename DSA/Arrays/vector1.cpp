// Vector introduction

#include <bits/stdc++.h>
using namespace std;

int main() {

    // int n;
    // cin >> n;

    /*
    vector<int> arr(n);
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
        // already seen in arrays
    }
    */

    /*
    vector<int> arr;
    for (int i = 0; i < n; i++) {
        int num;
        cin >> num;
        arr.push_back(num);
    }
    */

    /*
    vector<int> arr;
    cout << arr.empty();

    vector<int> arr1(5);
    cout << arr1.size();
    
    vector<int> arr2(5);
    arr2.push_back();
    cout << arr2.size();
    */

    vector<int> arr3(5);
    arr3.pop_back();
    cout << arr3.size();

    /*
    // 2d array/matrix
    vector<vector<int>> arr; //2D empty vector
    vector<vector<int>> arr(outer vector, inner vector);
    */

}
